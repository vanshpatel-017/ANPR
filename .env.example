# Base path where private internal dataset is mounted
ANPR_DATA_ROOT=C:/path/to/private/data

# Optional path to private production models
ANPR_MODEL_ROOT=C:/path/to/private/models

# Optional: if using Roboflow in experiments
ROBOFLOW_API_KEY=your_key_here
